@model TKPM_Project.Controllers.EditUserViewModel
@{
    ViewData["Title"] = "Edit User";
}

<div class="container">
    <h2>Edit User</h2>
    <form asp-controller="User" asp-action="Edit" method="post">
        @Html.AntiForgeryToken()
        <input type="hidden" asp-for="UserId" />

        <div class="form-group">
            <label asp-for="UserName">Username</label>
            <input asp-for="UserName" class="form-control" />
            <span asp-validation-for="UserName" class="text-danger"></span>
        </div>

        <div class="form-group">
            <label asp-for="Email">Email</label>
            <input asp-for="Email" class="form-control" />
            <span asp-validation-for="Email" class="text-danger"></span>
        </div>

        <div class="form-group">
            <label asp-for="FullName">Full Name</label>
            <input asp-for="FullName" class="form-control" />
            <span asp-validation-for="FullName" class="text-danger"></span>
        </div>

        <div class="form-group">
            <label>Roles</label>
            <!-- Hidden field to ensure SelectedRoles is always sent, even when empty -->
            <input type="hidden" name="SelectedRoles" value="" />
            <div>
                @if (Model.AllRoles != null)
                {
                    @for (int i = 0; i < Model.AllRoles.Count; i++)
                    {
                        <div class="form-check">
                            <input type="checkbox"
                                   name="SelectedRoles"
                                   value="@Model.AllRoles[i]"
                            @(Model.Roles.Contains(Model.AllRoles[i]) ? "checked" : "")
                                   class="form-check-input" id="role_@i" />
                            <label class="form-check-label" for="role_@i">@Model.AllRoles[i]</label>
                        </div>
                    }
                }
                else
                {
                    <p>No roles available</p>
                }
            </div>
        </div>

        <div class="mt-3">
            <button type="submit" class="btn btn-primary">Save</button>
            <a asp-action="UserManager" class="btn btn-secondary">Cancel</a>
        </div>
    </form>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <script>
        $(document).ready(function() {
            // For debugging form submission
            $('form').submit(function(e) {
                console.log('Form is submitting');
                // Remove this line if you want to see actual submission
                // e.preventDefault();

                // Log form data
                var formData = $(this).serializeArray();
                console.log('Form data:', formData);
            });
        });
    </script>
}